<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Navbar</title>
    <!-- Include Font Awesome for icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <!-- Custom CSS for the navbar -->
    <style>
        nav {
            background-color: #f5f5f5; /* Light gray background color */
            padding: 10px;
            text-align: center; /* Center-align the links */
        }

        .navbar-text {
            font-size: 18px;
            margin: 0 15px; /* Add spacing between links */
            color: #333; /* Dark text color */
            text-decoration: none; /* Remove underline from links */
        }

        .navbar-text:hover {
            color: #007bff; /* Blue color on hover */
        }
    </style>
</head>
<body>
    <nav>
        <a href="/" class="navbar-text"><i class="fas fa-home"></i> Home</a>
         {{#if jwt}}
            <a href="/secret" class="navbar-text"><i class="fas fa-user-secret"></i> Secret Page</a>
            <a href="/resetPassword" class="navbar-text"><i class="fas fa-user-secret"></i> Reset password</a>
            <a href="/logout" class="navbar-text"><i class="fas fa-sign-out-alt"></i> Logout</a>
        {{else if isGoogleAuth}}
             <a href="/auth/secret" class="navbar-text"><i class="fas fa-user-secret"></i> Secret Page</a>
            <a href="/auth/logout" class="navbar-text"><i class="fas fa-sign-out-alt"></i> Logout</a>
        {{else}}
            <a href="/registration" class="navbar-text"><i class="fas fa-user-plus"></i> Register</a>
            <a href="/login" class="navbar-text"><i class="fas fa-sign-in-alt"></i> Login</a>
        {{/if}}
    </nav>
</body>
</html>